#Calculate Variance :

# def variance(numbers): 
#     mean = sum(numbers) / len(numbers)
    
#     result = 0
    
#     for i in numbers:
#         result += (i - mean) ** 2
        
    
#     return result / len(numbers)